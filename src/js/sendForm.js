const form = document.querySelector('.modal__form');
console.dir(form);
const formBtn = document.querySelector('.form__btn');
console.dir(formBtn);
const formInput = document.querySelectorAll('.modal__form-input');
console.dir(formInput);
